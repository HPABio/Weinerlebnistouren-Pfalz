# Gmail Configuration for Contact Form
# Copy this content to a new file called .env in your project root

# Your Gmail address
GMAIL_USER=your-email@gmail.com

# Gmail App Password (NOT your regular Gmail password)
# To generate an App Password:
# 1. Go to your Google Account settings
# 2. Enable 2-Factor Authentication if not already enabled
# 3. Go to Security > App passwords
# 4. Generate a new app password for "Mail"
GMAIL_APP_PASSWORD=your-app-password-here

# Optional: Change the recipient email address
# RECIPIENT_EMAIL=hendrikcooper@gmx.de

# ─────────────────────────────────────────────────────────────────────────────
# Google Business Profile API (Google-Rezensionen)
#
# Nur nötig, wenn die Rezensionen automatisch geholt werden sollen
# (bun run reviews:fetch). Ohne diese Werte greift die manuelle Pflege
# in src/data/socialProof.ts.
#
# ACHTUNG: Die Business Profile APIs müssen für das Cloud-Projekt erst
# freigeschaltet werden — die Standardquote ist 0.
# https://developers.google.com/my-business/content/prereqs
# ─────────────────────────────────────────────────────────────────────────────

# OAuth-Client (Typ "Webanwendung"), Redirect-URI: http://localhost:5789/oauth2callback
GOOGLE_CLIENT_ID=xxxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Einmalig erzeugen mit: bun run reviews:auth
GOOGLE_REFRESH_TOKEN=your-refresh-token

# IDs anzeigen mit: bun run reviews:fetch --list
GOOGLE_ACCOUNT_ID=1234567890
GOOGLE_LOCATION_ID=9876543210
